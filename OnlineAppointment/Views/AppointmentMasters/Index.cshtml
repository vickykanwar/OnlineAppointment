@model IEnumerable<OnlineAppointment.Models.AppointmentMaster>

@{
    ViewBag.Title = "Index";
}



<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CustomerMaster.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ServiceMaster.ServiceName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.SlotTimeMaster.ID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Date)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.CustomerMaster.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ServiceMaster.ServiceName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.SlotTimeMaster.ID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Date)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID })
        </td>
    </tr>
}

</table>
